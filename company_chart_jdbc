package controller;

import java.io.IOException;
import java.util.ArrayList;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import model.chart_detail;

public class company_chart_jdbc extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException 
	{
		String tkr=request.getParameter("ticker1");
		ArrayList<Object> viewdata=new ArrayList<Object>();
		chart_detail vw=new chart_detail();
		viewdata=(ArrayList<Object>) vw.view(tkr);
		request.setAttribute("DataTable", viewdata);
		RequestDispatcher rd=request.getRequestDispatcher("/WEB-INF/views/company_detail_view.jsp");  
        rd.forward(request, response);  
	}
	
	
	
}
